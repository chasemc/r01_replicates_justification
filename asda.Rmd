---
title: "Untitled"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
---

```{r setup, include=FALSE}
library(flexdashboard)
library(crosstalk)
library(magrittr)
library(d3scatter)
```

### Chart A

```{r}
aa<-readRDS("data/replicates_plate.rds")


shared_mtcars <- SharedData$new(aa)


bscols(widths = c(3,NA,NA),
  list(
    filter_checkbox(unique(aa[!is.na(aa)]), shared_mtcars, ~cyl, inline = TRUE)
    

))
```


### Dataframe 1

```{r}

     shared_mtcars$data() %>% 
       kable(escape = F, align = "c", row.names = T) %>% 
       kable_styling(c("striped","bordered"), full_width = F,  font_size = 10) 


```